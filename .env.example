############################################################
# Local Development Defaults (.env.example)
# Copy to .env and adjust. DO NOT COMMIT the real .env file.
############################################################

APP_ENV=local
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=secret
DB_NAME=studentlogin
DB_CHARSET=utf8mb4
DB_SSL=0
DB_SSL_VERIFY=1
DB_STRICT=1
DB_DEBUG=1

# Uncomment to simulate production strict mode off for certain tests
# DB_STRICT=0

# SKYSQL JSON example (alternative single-var config). Use ONLY one style.
# SKYSQL_CREDS={"host":"serverless-eastus.sysp0000.db3.skysql.com","port":4048,"user":"dbpbf34656571","password":"CHANGEME","db":"studentlogin"}

# Base64 multi-line config example (APP_CONFIG_B64). Encode key=value lines.
# APP_CONFIG_B64=BASE64ENCODED_BLOCK

# Set to 1 temporarily for connection parameter logging (never in prod)
# DB_DEBUG=1

# Future (not yet implemented but reserved):
# RATE_LIMIT_WINDOW=60
# RATE_LIMIT_MAX=30

# End of file
